# Public publication projection

Only human-approved summaries belong here. Each file is a schema-validated Markdown
record generated after the second review gate. The Astro site reads this directory;
it has no access path to `data/private`.
