{
  "name": "deepgeno-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/reset-web-content-cache.mjs && npm run build --workspace @deepgeno/web",
    "dev": "npm run dev --workspace @deepgeno/web",
    "check": "npm run typecheck && npm test && npm run build && npm run artifact:check && npm run privacy && npm run deploy:dry-run",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "artifact:check": "node scripts/static-artifact-check.mjs",
    "privacy": "node scripts/privacy-check.mjs",
    "deploy:dry-run": "WRANGLER_LOG_PATH=.wrangler/logs wrangler deploy --dry-run",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "literature": "tsx packages/literature/src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1",
    "zod": "^4.4.3"
  }
}
